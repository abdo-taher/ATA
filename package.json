{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build && vite build --ssr"
    },
    "devDependencies": {
        "@popperjs/core": "^2.11.6",
        "laravel-vite-plugin": "^0.8.0",
        "postcss": "^8.4.14",
    }
}
